# Server-side only. Never prefix credentials with VITE_.
OPENAI_API_KEY=
LEXLATAM_MCP_TOKEN=
# Use the same secret as the MCP server's MCP_PRIVATE_TOKEN.
LEXLATAM_MCP_URL=http://localhost/mcp/
PORT=3001
